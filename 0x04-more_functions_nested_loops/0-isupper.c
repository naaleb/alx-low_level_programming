#include "main.h"

/**
  *_isupper - check if letter is uppercase
  *@c: number to be checked
  *Return: show 1 if upper letter or 0 if otherwise
  */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
		return (0);
}

#include "main.h"
#include <stdio.h>

/**
 * print_rev - Prints a string in reverse, followed by a new line.
 * @s: The input string.
 */

void print_rev(char *s)
{
	int i = 0;

	while (s[c] != '\0')
	{
	c++;
	}
	for (c -= 1; c >= 0; c--)
	{
		_puchar(s[c]);
	}
	_putchar('\n');
}
